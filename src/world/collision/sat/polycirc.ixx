export module polycirc;

import collider;
import query_info;

export namespace SAT {
QueryInfo queryPolyCirc(const Polygon &a, const Circle &b, const bool reverse) {
  // for (const Convex::Edge &edge : )
  return QueryInfo::NONE();
}
} // namespace SAT